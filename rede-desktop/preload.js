// Este app não precisa de nenhuma API do Node/Electron dentro da página —
// ele já funciona só com HTML/JS puro (igual no navegador). O preload fica
// vazio de propósito, mantendo contextIsolation ligado por segurança.
